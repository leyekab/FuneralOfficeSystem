@page "{id:int}"
@model FuneralOfficeSystem.Pages.BurialPlaces.EditModel

@{
    ViewData["Title"] = "Επεξεργασία Κοιμητηρίου";
}

<div class="container">
    <h1 class="text-center mb-4">Επεξεργασία Κοιμητηρίου</h1>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-body">
                    <form method="post" id="editForm">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <input type="hidden" asp-for="BurialPlace.Id" />

                        <div class="form-group mb-3">
                            <span class="text-danger">*</span>
                            <label asp-for="BurialPlace.Name" class="control-label">Όνομα</label>
                            <input asp-for="BurialPlace.Name" class="form-control" required />
                            <span asp-validation-for="BurialPlace.Name" class="text-danger"></span>
                        </div>

                        <div class="form-group form-check mb-3">
                            <input asp-for="BurialPlace.IsEnabled" class="form-check-input" />
                            <label asp-for="BurialPlace.IsEnabled" class="form-check-label">Ενεργό</label>
                        </div>

                        <div class="row mt-4">
                            <div class="col-md-6">
                                <a asp-page="./Index" class="btn btn-secondary w-100">Επιστροφή στη Λίστα</a>
                            </div>
                            <div class="col-md-6">
                                <input type="submit" value="Αποθήκευση" class="btn btn-primary w-100" id="saveButton" />
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        // Προσθήκη client-side κώδικα για να επιβεβαιώσουμε ότι η φόρμα υποβάλλεται σωστά
        $(document).ready(function () {
            $("#editForm").on("submit", function (e) {
                console.log("BurialPlace edit form submitted");
                // Επιστρέφουμε true για να επιτρέψουμε την κανονική υποβολή της φόρμας
                return true;
            });
        });
    </script>
}